# Automatically generated on 2020-09-09 UTC from https://codemagic.io/app/5f57eeabe2db2707c9eebe5d/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 30
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: codemagic_ci
          include: true
          source: false
    scripts:
      - name: all the things
        script: |
          flutter emulators --launch emulator
      - name: Set up debug keystore
        script: |
          rm -f ~/.android/debug.keystore
          keytool -genkeypair \
            -alias androiddebugkey \
            -keypass android \
            -keystore ~/.android/debug.keystore \
            -storepass android \
            -dname 'CN=Android Debug,O=Android,C=US' \
            -keyalg 'RSA' \
            -keysize 2048 \
            -validity 10000
      - name: Setup local properties
        script: |
          echo "flutter.sdk=$HOME/programs/flutter" > "$FCI_BUILD_DIR/android/local.properties"
          cd . && flutter packages pub get
          cd . && flutter analyze --write=analyzer-output.txt
          cd . && flutter drive --target=test_driver/complete_guide_app.dart
          cd . && flutter build apk --debug
      - name: Unit tests
        script: |
          # What about coverage?
          mkdir -p test-results
          flutter test --machine > test-results/flutter.json
        test_report: test-results/flutter.json
    artifacts:
      - build/**/outputs/**/*.apk
      - build/**/outputs/**/*.aab
      - build/**/outputs/**/mapping.txt
      - flutter_drive.log
      - analyzer-output.txt
    publishing:
      email:
        recipients:
          - randy@eppingerconsulting.com
